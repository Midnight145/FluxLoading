
name: Build and test

on:
  pull_request:
    branches: [ master, main, dev, 1.7.10, 'project/**' ]
  push:
    branches: [ master, main, dev, 1.7.10, 'project/**' ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
